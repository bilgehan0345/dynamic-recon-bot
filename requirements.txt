# Required external packages to run the project
# Installation command: pip install -r requirements.txt

httpx          # For asynchronous HTTP requests
python-dotenv  # For loading environment variables from .env